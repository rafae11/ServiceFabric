version: 1.0.{build}
image: Visual Studio 2019
configuration: Release
platform: Any CPU

before_build:
  - nuget restore
  - choco install opencover.portable
  - choco install codecov

build:
  verbosity: detailed
  project: Services\The Method\Microservice\ServiceModelEx.ServiceFabric\Microservice.sln

notifications:
- provider: Email
  to:
  - rafae11@yahoo.com
  on_build_success: true
  on_build_failure: true
  on_build_status_changed: true